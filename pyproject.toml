[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
log_cli = true
log_cli_level = "INFO"
log_format = "%(asctime)s [%(levelname)s] %(name)s - %(message)s"
markers = [
    "integration: 실제 네트워크에 연결하는 통합 테스트 (pytest -m integration 으로 선택 실행)",
]
